meta {
  name: POST inventory item (Create new SKU item)
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/inventory/{{skuId}}
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "quantity": 10
  }
}

settings {
  encodeUrl: false
}
